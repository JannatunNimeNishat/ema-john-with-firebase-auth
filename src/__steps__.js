/**
 * 1. create firebase project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export default app 
 * 5. Build > Authentication > get started > enable sign in method 
 * 6. Create sign up and Login route
 */

/** CONTEXT API (share data)
 * 1. create a context Provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 */

/** FireBase HOSTING
 * One time per PC/laptop
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * 
 * 
 * For each project one time
 * 1. firebase init
 * 2. (y) proceed 
 * 3. Hosting: Configure files for Firebase Hosting and (optionally) set up GitHub Action deploys 
 *    (up and down arrow) use space bar to select
 * 4. Use an existing project
 * 5. select the project carefully
 * 6. which project as public directory : dist (vite)
 * 7. Configure as a single-page app: y
 * 8. Set up automatic builds and deploys with GitHub? (y/N): n
 * 
 * 
 * For every time deploy (new project or update project)
 * 1. npm run build
 * 2. firebase deploy
 * 
 * 
 */

